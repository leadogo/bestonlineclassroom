# Launch: 2026-09-21, 5:00 PM Mountain

The first session in our own room. Checklist frozen at 16:30 MT; notes added through the evening.

## Before 16:30 MT
- [ ] `bestonlineclassroom.com` resolves to Vercel (Cloudflare record DNS-only); `www` redirects to the bare domain.
- [ ] Production env (Doppler prd → Vercel): every name in `.env.example` filled; `vercel env ls production`.
- [ ] Video plays from the domain on a phone: `/w/ailg-r?at=100&key=…` (sound on tap, no player bar).
- [ ] Countdown → live flip: open a token link at 16:58 MT, watch it start at 17:00:00 with no reload.
- [ ] William signed in at `/mod` on his phone; a test reply shows in a second browser within 3 s.
- [ ] Slack relay seen in #autoweb-chat from a Test Sample message.
- [ ] Site: `EASYWEBINAR_PARALLEL=1` on; EasyWebinar reminders left on; last Test Sample opt-in (CP4) still passes.
- [ ] Room count: leadogo's cron still reads `room_join` from the site's `/join` (unchanged).

## 16:55 to 17:10 MT
- [ ] Room open in two browsers (phone + laptop) as Test Sample; `attendance` rows appear.
- [ ] Watch the first ten real joins in `/mod`'s "In the room now" strip; any "Reconnecting…" reports in chat.

## During
- 17:30 and 18:30 MT: `select count(distinct registrant_id) from attendance where session_date = '2026-09-21'`
  against the room-count post in #autoweb-intel.
- CTA at 1:15:00 (18:15 MT): `cta_clicked_at` count at 18:20.

## After
- Peak concurrent, joins, chat volume, CTA clicks, blocks. What broke, what was slow, what people asked in chat.
- Decide: EasyWebinar parallel run continues until the Zap is re-pointed (William).

## Notes
(added on the night)
